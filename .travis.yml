language: python
python:
    - '2.7'
install:
    - sudo apt-get -qq update 
    - sudo apt-get install -y zip
    - pip install azure-cli
script:
    - ./package.sh
    - az --version
    - az container list
